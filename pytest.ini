[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Third-party (FastAPI/Starlette) deprecation warnings are noise we don't own;
# keep them visible but never let them fail the suite.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
